---
apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    name: node
  name: node-controller
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: node
    spec:
      containers:
      - image: dockerhandson/java-web-app
        name: node
        ports:
        - name: node
          containerPort: 3000  
---
# Node Port Service
apiVersion: v1
kind: Service
metadata:
  labels:
    name: node
  name: node
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    name: node
